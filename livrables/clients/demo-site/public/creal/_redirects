# Routage de boutiquecreal.com
#
# Liste EXPLICITE, tenue à la main. NE JAMAIS mettre d'attrape-tout ici
# (« /* » ou « /:slug »). Il serait évalué AVANT le routage naturel de
# Cloudflare Pages et capturerait tout ce qu'on oublie de déclarer, y compris
# la page 404. Un lien partagé tombe alors sur la mauvaise page, sans qu'aucune
# erreur ne soit levée nulle part : le défaut ne se voit qu'en le testant.
#
# Cette ligne existe parce que le test l'a exigée : depuis l'ajout de
# 404.html, Cloudflare Pages ne redirige plus /admin vers /admin/ tout seul,
# il sert la page 404. Or /admin sans barre oblique finale est précisément la
# forme qu'on écrit à la main dans un message WhatsApp. R-53.

/admin    /admin/    301
/guide    /guide/    301
